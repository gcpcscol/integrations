name: example
display_name: Example
description: Import files from local filesystem
homepage: https://github.com/tracepanic/plakar-integration
license: ISC
connectors:
  - type: importer
    executable: ./test-importer
    protocols: [test]
    flags: [localfs]
  - type: exporter
    executable: ./test-exporter
    protocols: [test]
    flags: [localfs]
  - type: storage
    executable: ./test-storage
    protocols: [test]
    flags: [localfs]
